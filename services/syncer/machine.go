package syncer

import (
	"github.com/unpackdev/downloader/pkg/machine"
)

func RegisterMachineStates(sm *machine.StateMachine) error {
	//sm.RegisterState(unpacker.DiscoverState, unpacker.NewDiscoverContractHandler(s.ctx, s.unpacker))
	return nil
}
