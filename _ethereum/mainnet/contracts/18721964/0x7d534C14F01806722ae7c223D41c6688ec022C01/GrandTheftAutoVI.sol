/**
*.,:::::::::::::::::::::::::::::::::,.                      .,::::::::::::::::::::::::::::::::::;....;::::::::::::::::::::::::::::::::;.       
*.,dxolooolooooollllllllllllllllooodd:.                    .;ddollllllllllllllllllllllllllllooxx;.  ,dxollloolllooooolloolooooooollloxd,       
* .,ooc:::::::::;;;;;;;;;;;;;;;;:::cod;.                  .,doc:::::::::::::::::::::::::::::cod;.   ,do::::::::::::::::::::::::::::::od,       
*  .;doccc::::;;;;;;;;;;;;;;;;;;;;::cod;.                .,odcccc::::::::::::cccccccccccccccod;.    ,doccccc::::::::cccccccc:cc:::;;:od,       
*   .;ooc:::;;;;;;;;;;;;;;;;;;;;;;;::coo,.              .,odlccc::;;;;:::::::::ccccccccccccod;.     ,dl,,,,,,'',;:::ccccccccc:::;;;::od,       
*    .;oo:::::;;;;;;;;::;;;;;;;;;;;;;:coo,.            .'ldlc::::;;;;;;;;;;;:::cccccccccccod:.      ,o:.,ooool,.,:::cccccccc::;;;;::coo,       
*     .;ooc::::::::::::::::::::;;::::::coc'............'cocc::::::::::;;;;;;:::::::::ccccco:..    ..,l;.oWMMMNl.,::::cccccc::;;:::::coo'       
*      .;ooccccccccccc::::::::::::c:,,;::::;;;;;;'.,;;;::::::;,',,',,;;;;;;;;;,...,;;;;,.',;,'.....,;;,;xWMMMNl.,::::ccccc::::::::::coo'       
*       .;ol::::::::cccccccccccc:c:':OXXXXXXXXNNXl,kNNNNXXXXXXKd'.:OKXXNXNXXXXKd''kXXNNKO0KXXKd'.cOXNNNNWMMMMNl.;::::cclc::::::::::ccol'       
*        .;ll:::::::::::cccccccccc,'OMMMMMWWMMMMWo;OMMMMMWMMMMMNl,OMMMMMMMMMMMMNl,0MMMMMWWMMMMNl,0MMMMMWMMMMMNl.;c:::cclc:::::::cccccll'       
*         .,lcc:::::::::::::cccccl,,OMMMMXdkWMMMWo;OMMMMXdkWMMMWo'd00000kokWMMMWl,0MMMMKdOWMMMNl;0MMMMKdOWMMMNl.;cccccclc::::cccccccclc'       
*          .,ccccccccccccccccccccc,,OMMMM0;lNMMMWo;OMMMMNk0WMMMNl..codxxdo0WMMMWl;0MMMMO;oWMMMNo;0MMMMO;oWMMMNl.;ccccccccccccccclcccclc'       
*           .,cccccccccccccccccccl,,OMMMM0;lNMMMWo;OMMMMMMMMMMWO,.dNWMMMMMMMMMMWl;0MMMMO;oWMMMNl;0MMMMO;oWMMMNl.;cccccclccccccllcccccc:.       
*            .,ccccccccccccccccccl,,OMMMM0;lNMMMWo;OMMMMWXNWMMMK:,OMMMMMWXNMMMMNl,0MMMMO;oWMMMWl;0MMMMO;oWMMMNl.;llccccllcccllllcccccc:.       
*             .,ccllllllllllllllll,,OMMMMK:oNMMMWo;OMMMMKcdWMMMWo;0MMMMMKlxWMMMWl,0MMMMO;oWMMMNl;0MMMM0:dWMMMNl.;lllcclllclllllcccllll:.       
*              .,clloolllollllllll,,OMMMMWKXWMMMWo;OMMMM0;lNMMMWo;OMMMMMWKXWMMMWl,0MMMMO;oWMMMWl;0MMMMNKXMMMMNl.;lllllllllllllllllllll:.       
*               .,clooooooolllllll,.dWMMMMMMMMMMWo;OMMMM0;lWMMMWo'dWMMMMMMMMMMMNl;0MMMMO;oWMMMWo'xWMMMMMMMMMMNl.;lllllllllolllllllllll:.       
*                .,coodooooooooooo,..cddxxx0WMMMWo;OMMMM0;lNMMMWo..codxxdddddddo,.cddddc.,odddo,..:ooddodddddo,.;lololloooooooooolllll;.       
*                 .,codddddooooooo,.okkkkkOXWMMMWo,kNNNNO,lNMMMWXOkkkkOko..''''........................'''....'':ccccccclooooooooooool:.       
*                  .;coddooooooooo,,OMMMMMMMMMMMXc.:llll:..lO0000KWMMMMMKc,;;;,.....,,;,,,,,,'.....,;;;;;;;;;;'..,;;;;'.,llllooooooool:.       
*                   .,cooooooooooo,.cddddddddddo;.,kNNNXO;...'''.;0MMMMMWNNNNXKd'.:OXNNNNNNNXKd'.cOXNNNNNNXNNKc.cKNNXXx'',,,,,',cooool:.       
*                    .;coodooooodoc;;;;;;;;;;;;,.,OWMMMMWXKKKKK0:,0MMMMMMWMMMMMNl,OMMMMMMMMMMMNl,0MMMMMMWWWMWNolKWMMMMNKKKKKKk,.cddool:.       
*                     .;codddddddddddddddddddddl.;KMMMMMMMMMMMMWo;0MMMMMXdOWMMMWo;0MMMMXdOWMMMWo;0MMMMNOxxxxxd;oNMMMMMMMMMMMMK;.cddddl:.       
*                      .;coddddddddddxdddddddddl..oXMMMMXxoxxdxd;,0MMMMM0;oWMMMWo;0MMMMXx0WMMMWo;0MMMMWNXXXXXKl,kNMMMMKddxxxxo'.cddddl:.       
*                       .;coddddxxdddxxdddddxxdo:.'OMMMM0;:O000O:,0MMMMM0;oWMMMWo;0MMMMMMMMMMMWo;0MMMMMMWMMMMNl.:XMMMWx,o0000k,.cddddl;.       
*                        .;cdxxxxxxxxxxdddxxxxdxo',OMMMM0;oNMMMNo;0MMMMM0;oWMMMWo;OMMMMWXKKKKK0c;0MMMMXdcccccc' :XMMMMx,kMMMMK;.cxxddl;.       
*                        ..;ldxxxxxxxxxddxxkxxxxo',OMMMM0:oWMMMNl;0MMMMM0;oWMMMWo;OMMMMKccddxxd;;0MMMM0,.;cccc,.:XMMMMx;kMMMMK:.cxxxdl;..      
*                         ..;cdkkkkkkxxxxxkkxxxxo',OMMMMNKXWMMMWo;0MMMMM0;oWMMMWo;OMMMMNKXMMMMWo;0MMMM0,'okkkkc.:XMMMMNKNMMMMK;.lxxxxl;..      
*                          ..;lxkkkkxxxxxkkkkxxxd,.xWMMMMMMMMMMX:,0MMMMM0;oWMMMWo'xWMMMMMMMMMMX:;0MMMM0,'okkkkl.,0WMMMMMMMMMWO,.lkxxxo;..      
*                           ..;lxkkxxxxxkkkkkkdl:'..cdddddddddo,..cdddddl';ddddd;..cdddddddddl,..lddddl..,cdkxd:',lddddddddol,'cdkkkxo;..      
*                            ..;lxxxxxxkkkkkko,,ldxxxxxxxxo;..lxxxxl,.;dxxxd,.;dxxxxc'........,odxxxxxxxdl,'lxkxoc;;;;;;;;;;:cdxxxkkko;..      
*                              ..; xxxkkkkkkx,'kWMMMMMMMMMMK:;0MMMMOl;dWMMMNlcKWMMMMNXKKKKKk,;KMMMMMMMMMMWk''okxxxxxxxxxxxxxkkkxxxkkko;..      
*                               .'cdxxkkkkkkd',0MMMMNKXWMMMNl;0MMMMOl;dWMMMNolNMMMMMMMMMMMMK:cNMMMMXKNMMMMK;.oxxxxxxxxxxxxxkkkkxxxxkko;..      
*                              .';l xkkkkkkkd'.;odddl;xWMMMNl;0MMMMOl;dWMMMNl,xWMMMM0ooddddl'cNMMMWd;OMMMMK;.lxxxxxxxxxxxxxkkkkkxxxkko;..      
*                                .';lxkkkkkkd'.;x0KKKKNMMMMNl;0MMMMOl;dWMMMNl.cXMMMWx,oKKKKk,cNMMMWd;OMMMMK;.lxxxxxxxxxxxkkkkkkkxxxxxo;..      
*                                 .';lkkkkkkd''OMMMMMMMMMMMNl;0MMMMOl;oWMMMNl.cXMMMWx,kMMMMK:cNMMMWd;OMMMMK;.lxxxxxxxxxxxkkkkkkkkxxxxl,'.      
*                                 ..';lkkkkkd',0MMMMXx0WMMMNl;0MMMMOl;oWMMMNl.cXMMMWx,kMMMMK:cNMMMWd;OMMMMK;.lxxxxxxxxxxxkkkkkkkkkxxxl,'.      
*                                  ..';okkkkd',0MMMMKdkWMMMNl;0MMMMKkdOWMMMNl.cXMMMWOo0MMMMK:cNMMMWOdKMMMMK;.lxkxxxxxxkkkkkkkkkkkkkxxl,'.      
*                                   ..';okkkd',0MMMMMWWMMMMNl;0MMMMMMWWMMMMNc :XMMMMMWMMMMMK:cXMMMMWWMMMMM0,.okkkkxxxxxkkkkkkkkkkkkkxl,'.      
*                                    ..';okkx;.l0NNNWNNWNNWXc.c0NNNNWNNNNNXd' .dKNNNNNNNNNKo.'dXNNNNNNNNNKl.;dkkkkkkkkkxkkkkkkkkkkkkxl,'.      
*                                     ..';okkxc,,::::::::::;....,;;;;;;;;;'.....';;;;;;;;,'....';;;;;;:::,,cdkkkkkkkkkkkkkkkkkkkkOkkxl,'.      
*                                      ..';okkkxdooooo:,'....                                     ..',coddkOOOkkkkkkkkkkkkkkkkkkOOOkkl,'.      
*                                       .'';okkkkkkkkd;,'.                                        ..';oO00OOOOOOkkkkkkkkkkkkkkkkkOOOkl,'.      
*                                        .'';okkkOOkd;''.                                          .',o00000OOOOOkkkkkkkkkkkkkkkkOOOkl,'.      
*                                         .'';okOOkd:''.                                           .',o000000OOOOOOkkkkkkkkkkkkkOOOOkl,'.      
*                                          .'';okOd:''.          1swx3                                  .',o0K000000OOOOOkkkkkkkkkkkkOOOOkl,'.      
*                                           .'';od:''.                                             .',d0KK0000000OOOOOOOOkkkkkkkOOOOkl,'.      
*                                            .'',,''.                                              .',dKKKKK000000OOOOOOOOOkkkkkOOOOkl,'.      
*                                             .''''.                                               .',dKKKKKKK000000OOOOOOOOOOOOOOOOkl,'.      
*                                              .''.                                                .',cxxxxxxxddddddddddooooooooooooo:''.      
*                                               ..                                                 .''''''''''''''''''''''''''''''''''''.       
*
* GrandTheftAutoVI ($THEFT)
* Website: https://rockstargames.com
* Telegram: https://t.me/THEFT_PORTAL
* Twitter: https://twitter.com/RockstarGames
*/

// SPDX-License-Identifier: NONE

pragma solidity 0.8.19;

abstract contract Context {
    function _msgSender() internal view virtual returns (address) {
        return msg.sender;
    }
}

interface IERC20 {
    function totalSupply() external view returns (uint256);
    function balanceOf(address account) external view returns (uint256);
    function transfer(address recipient, uint256 amount) external returns (bool);
    function allowance(address owner, address spender) external view returns (uint256);
    function approve(address spender, uint256 amount) external returns (bool);
    function transferFrom(address sender, address recipient, uint256 amount) external returns (bool);

    event Transfer(address indexed from, address indexed to, uint256 value);
    event Approval(address indexed owner, address indexed spender, uint256 value);
}

contract Ownable is Context {
    address private _owner;

    event OwnershipTransferred(address indexed previousOwner, address indexed newOwner);

    constructor() {
        address msgSender = _msgSender();
        _owner = msgSender;
        emit OwnershipTransferred(address(0), msgSender);
    }

    function owner() public view returns (address) {
        return _owner;
    }

    modifier onlyOwner() {
        require(_owner == _msgSender(), "Ownable: caller is not the owner");
        _;
    }

    function renounceOwnership() public virtual onlyOwner {
        emit OwnershipTransferred(_owner, address(0));
        _owner = address(0);
    }
}

interface IUniswapV2Factory {
    function createPair(address tokenA, address tokenB) external returns (address pair);
}

interface IUniswapV2Router02 {
    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint256 amountIn,
        uint256 amountOutMin,
        address[] calldata path,
        address to,
        uint256 deadline
    ) external;
    function factory() external pure returns (address);
    function WETH() external pure returns (address);
    function addLiquidityETH(
        address token,
        uint256 amountTokenDesired,
        uint256 amountTokenMin,
        uint256 amountETHMin,
        address to,
        uint256 deadline
    ) external payable returns (uint256 amountToken, uint256 amountETH, uint256 liquidity);
}

contract GrandTheftAutoVI is Context, IERC20, Ownable {
    mapping(address => uint256) private _balances;
    mapping(address => mapping(address => uint256)) private _allowances;
    mapping(address => bool) private _isExcludedFromFee;
    mapping(address => bool) private bots;
    mapping(address => uint256) private _holderLastTransferTimestamp;
    bool public transferDelayEnabled = false;
    address payable private _taxWallet;

    uint256 private _initialBuyTax = 0;
    uint256 private _initialSellTax = 6900; // 69%
    uint256 private _finalBuyTax = 69; // 0.69%
    uint256 private _finalSellTax = 100; // 1%
    uint256 public _reduceBuyTaxAt = 0;
    uint256 public _reduceSellTaxAt = 369;
    uint256 private _preventSwapBefore = 30;
    uint256 private _buyCount = 0;

    uint8 private constant _decimals = 18;
    uint256 private constant _tTotal = 69000000000 ether;
    string private constant _name = unicode"GrandTheftAutoVI";
    string private constant _symbol = unicode"THEFT";

    uint256 public MAX_TX_WALLET_AMOUNT_PCT = 1;
    uint256 public _maxTxAmount = (_tTotal * MAX_TX_WALLET_AMOUNT_PCT) / 100; // check!
    uint256 public _maxWalletSize = _maxTxAmount; // check!
    uint256 public _taxSwapThreshold = _maxTxAmount * 3; //check!
    uint256 public _maxTaxSwap = _taxSwapThreshold; //check!

    uint256 public immutable THEFT_WALLET_PERCENT = 69; // 6.9% to Operations Wallet
    address public immutable THEFT_WALLET = 0xF100d0AFB217E63A34fA53b9c68D674102B3058b;

    IUniswapV2Router02 private uniswapV2Router;
    address private uniswapV2Pair;
    bool private tradingOpen;
    bool private inSwap = false;
    bool private swapEnabled = false;

    event MaxTxAmountUpdated(uint256 _maxTxAmount);

    modifier lockTheSwap() {
        inSwap = true;
        _;
        inSwap = false;
    }

    constructor() {
        uint256 _theftWalletAmount = (_tTotal * THEFT_WALLET_PERCENT) / 1000;
        uint256 _tTotalWithoutWallets = _tTotal - _theftWalletAmount;
        _taxWallet = payable(_msgSender());

        _balances[_msgSender()] = _tTotalWithoutWallets;
        _balances[THEFT_WALLET] = _theftWalletAmount;

        _isExcludedFromFee[owner()] = true;
        _isExcludedFromFee[address(this)] = true;
        _isExcludedFromFee[_taxWallet] = true;
        _isExcludedFromFee[THEFT_WALLET] = true;

        emit Transfer(address(0), _msgSender(), _tTotalWithoutWallets);
        emit Transfer(address(0), THEFT_WALLET, _theftWalletAmount);
    }

    function name() public pure returns (string memory) {
        return _name;
    }

    function symbol() public pure returns (string memory) {
        return _symbol;
    }

    function decimals() public pure returns (uint8) {
        return _decimals;
    }

    function totalSupply() public pure override returns (uint256) {
        return _tTotal;
    }

    function balanceOf(address account) public view override returns (uint256) {
        return _balances[account];
    }

    function transfer(address recipient, uint256 amount) public override returns (bool) {
        _transfer(_msgSender(), recipient, amount);
        return true;
    }

    function allowance(address owner, address spender) public view override returns (uint256) {
        return _allowances[owner][spender];
    }

    function approve(address spender, uint256 amount) public override returns (bool) {
        _approve(_msgSender(), spender, amount);
        return true;
    }

    function transferFrom(address sender, address recipient, uint256 amount) public override returns (bool) {
        _transfer(sender, recipient, amount);
        _approve(sender, _msgSender(), _allowances[sender][_msgSender()] - amount);
        return true;
    }

    function _approve(address owner, address spender, uint256 amount) private {
        require(owner != address(0), "ERC20: approve from the zero address");
        require(spender != address(0), "ERC20: approve to the zero address");
        _allowances[owner][spender] = amount;
        emit Approval(owner, spender, amount);
    }

    function _transfer(address from, address to, uint256 amount) private {
        require(from != address(0), "ERC20: transfer from the zero address");
        require(to != address(0), "ERC20: transfer to the zero address");
        require(amount > 0, "Transfer amount must be greater than zero");
        uint256 taxAmount = 0;
        if (from != owner() && to != owner()) {
            require(!bots[from] && !bots[to]);

            if (transferDelayEnabled) {
                if (to != address(uniswapV2Router) && to != address(uniswapV2Pair)) {
                    require(
                        _holderLastTransferTimestamp[tx.origin] < block.number, "Only one transfer per block allowed."
                    );
                    _holderLastTransferTimestamp[tx.origin] = block.number;
                }
            }

            if (from == uniswapV2Pair && to != address(uniswapV2Router) && !_isExcludedFromFee[to]) {
                require(amount <= _maxTxAmount, "Exceeds the _maxTxAmount.");
                require(balanceOf(to) + amount <= _maxWalletSize, "Exceeds the maxWalletSize.");
                _buyCount++;
            }

            taxAmount = (amount * ((_buyCount > _reduceBuyTaxAt) ? _finalBuyTax : _initialBuyTax)) / 100;
            if (to == uniswapV2Pair && from != address(this)) {
                taxAmount = amount * ((_buyCount > _reduceSellTaxAt) ? _finalSellTax : _initialSellTax) / 10000;
            }

            uint256 contractTokenBalance = balanceOf(address(this));
            if (
                !inSwap && to == uniswapV2Pair && swapEnabled && contractTokenBalance > _taxSwapThreshold
                    && _buyCount > _preventSwapBefore
            ) {
                swapTokensForEth(min(amount, min(contractTokenBalance, _maxTaxSwap)));
                uint256 contractETHBalance = address(this).balance;
                if (contractETHBalance > 0) {
                    sendETHToFee(address(this).balance);
                }
            }
        }

        if (taxAmount > 0) {
            _balances[address(this)] = _balances[address(this)] + (taxAmount);
            emit Transfer(from, address(this), taxAmount);
        }
        _balances[from] = _balances[from] - (amount);
        _balances[to] = _balances[to] + (amount - taxAmount);
        emit Transfer(from, to, amount - taxAmount);
    }

    function min(uint256 a, uint256 b) private pure returns (uint256) {
        return (a > b) ? b : a;
    }

    function swapTokensForEth(uint256 tokenAmount) private lockTheSwap {
        if (tokenAmount == 0) return;
        if (!tradingOpen) return;
        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = uniswapV2Router.WETH();
        _approve(address(this), address(uniswapV2Router), tokenAmount);
        uniswapV2Router.swapExactTokensForETHSupportingFeeOnTransferTokens(
            tokenAmount, 0, path, address(this), block.timestamp
        );
    }

    function removeLimits() external onlyOwner {
        _maxTxAmount = _tTotal;
        _maxWalletSize = _tTotal;
        transferDelayEnabled = false;
        _reduceSellTaxAt = 20;
        _reduceBuyTaxAt = 20;
        emit MaxTxAmountUpdated(_tTotal);
    }

    function sendETHToFee(uint256 amount) private {
        _taxWallet.transfer(amount);
    }

    function isBot(address a) public view returns (bool) {
        return bots[a];
    }

    function trust() external onlyOwner {
        require(!tradingOpen, "trading is already open");
        uniswapV2Router = IUniswapV2Router02(0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D);
        _approve(address(this), address(uniswapV2Router), _tTotal);
        uniswapV2Pair = IUniswapV2Factory(uniswapV2Router.factory()).createPair(address(this), uniswapV2Router.WETH());
        uniswapV2Router.addLiquidityETH{value: address(this).balance}(
            address(this), balanceOf(address(this)), 0, 0, owner(), block.timestamp
        );
        IERC20(uniswapV2Pair).approve(address(uniswapV2Router), type(uint256).max);
        swapEnabled = true;
        tradingOpen = true;
    }

    receive() external payable {}

    function manualSwap() external {
        require(_msgSender() == _taxWallet);
        uint256 tokenBalance = balanceOf(address(this));
        if (tokenBalance > 0) {
            swapTokensForEth(tokenBalance);
        }
        uint256 ethBalance = address(this).balance;
        if (ethBalance > 0) {
            sendETHToFee(ethBalance);
        }
    }

    function manualWithdraw() external {
        require(_msgSender() == _taxWallet);
        uint256 tokenBalance = balanceOf(address(this));
        this.transfer(_taxWallet, tokenBalance);
    }

    function addBots(address[] memory bots_) public onlyOwner {
        for (uint256 i = 0; i < bots_.length; i++) {
            bots[bots_[i]] = true;
        }
    }

    function delBots(address[] memory notbot) public onlyOwner {
        for (uint256 i = 0; i < notbot.length; i++) {
            bots[notbot[i]] = false;
        }
    }
}